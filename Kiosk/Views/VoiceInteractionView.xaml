<UserControl x:Class="Kiosk.Views.VoiceInteractionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Kiosk.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Style="{StaticResource VoicePanelContainerStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,0" HorizontalAlignment="Left">
                <TextBlock Text="🖥️" FontSize="26" Margin="0,0,10,0"/>
                <Border Style="{StaticResource VoiceTtsBubbleStyle}" Margin="0,2,0,0">
                    <TextBlock Text="{Binding TTSMessage}"
                               Style="{StaticResource VoiceTtsTextStyle}" TextWrapping="Wrap"/>
                </Border>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,0" HorizontalAlignment="Right">
                <Border Style="{StaticResource VoiceSttBubbleStyle}" Margin="0,2,10,0">
                    <TextBlock Text="{Binding STTMessage}"
                               Style="{StaticResource VoiceSttTextStyle}" TextWrapping="Wrap"/>
                </Border>
                <TextBlock Text="💬" FontSize="26"/>
            </StackPanel>
        </Grid>
    </Border>

</UserControl>
