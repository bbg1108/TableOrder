<UserControl x:Class="Kiosk.Views.Popups.PaymentSuccessPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Kiosk.Views.Popups"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="결제에 성공했습니다" FontSize="20" FontWeight="Bold"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding TimeCount}" Grid.Column="0" Foreground="Red" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text="초 뒤에 첫화면으로 이동합니다" Grid.Column="1" FontSize="20" FontWeight="Bold"/>
        </Grid>
    </StackPanel>
</UserControl>
