<UserControl x:Class="Kiosk.Views.SideBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Kiosk.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid DataContext="{Binding SideBarViewModel}">
        <StackPanel Orientation="Horizontal">
            <Button Content="⌂ 첫화면" Command="{Binding HomeButtonCommand}" Style="{StaticResource SlateGrayButtonStyle}" Width="100"/>
        </StackPanel>
    </Grid>
</UserControl>
