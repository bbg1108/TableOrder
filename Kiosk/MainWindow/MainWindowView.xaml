<Window x:Class="Kiosk.MainWindow.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kiosk.MainWindow"
        xmlns:view="clr-namespace:Kiosk.Views"
        xmlns:converters="clr-namespace:Kiosk.Converters"
        xmlns:vm="clr-namespace:Kiosk.ViewModels"
        mc:Ignorable="d"
        Title="Kiosk" ResizeMode="NoResize"
        Width="1280" Height="800">
    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
        <Grid>
            <ContentControl Content="{Binding CurrntViewModel}">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type vm:FirstViewModel}">
                        <view:FirstView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vm:KioskMainViewModel}">
                        <view:KioskMainView/>
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>
        </Grid>
</Window>
