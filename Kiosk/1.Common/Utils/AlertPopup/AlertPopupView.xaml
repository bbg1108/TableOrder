<Window x:Class="Kiosk.AlertPopupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kiosk"
        mc:Ignorable="d"
        Title="title" MaxHeight="{Binding Height}" MaxWidth="{Binding Width}"
        ResizeMode="NoResize" WindowStyle="None">
    <Grid>
        <Border BorderBrush="Gray" BorderThickness="1">
            <ContentControl Content="{Binding}" Style="{StaticResource BasePopupStyle}">
                <ContentControl.ContentTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Margin="0 0 0 30"
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </DataTemplate>
                </ContentControl.ContentTemplate>
            </ContentControl>
        </Border>
    </Grid>
</Window>
