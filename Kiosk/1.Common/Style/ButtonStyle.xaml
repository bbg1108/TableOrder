<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- 컬러 정의 -->
    <Color x:Key="ThemeRed">#C62828</Color>
    <Color x:Key="ThemeRedLight">#E57373</Color>
    <Color x:Key="ThemeSlateGray">SlateGray</Color>
    <Color x:Key="ThemeSlateGrayLight">#82919E</Color>
    <Color x:Key="ThemeGray">#616161</Color>
    <Color x:Key="ThemeGrayLight">#9E9E9E</Color>
    <Color x:Key="ThemeGreen">#3CB371</Color>
    <Color x:Key="ThemeGreenLight">#7ED6A5</Color>

    <SolidColorBrush x:Key="ThemeRedBrush" Color="{StaticResource ThemeRed}" />
    <SolidColorBrush x:Key="ThemeRedLightBrush" Color="{StaticResource ThemeRedLight}" />
    <SolidColorBrush x:Key="ThemeSlateGrayBrush" Color="{StaticResource ThemeSlateGray}" />
    <SolidColorBrush x:Key="ThemeSlateGrayLightBrush" Color="{StaticResource ThemeSlateGrayLight}" />
    <SolidColorBrush x:Key="ThemeGrayBrush" Color="{StaticResource ThemeGray}" />
    <SolidColorBrush x:Key="ThemeGrayLightBrush" Color="{StaticResource ThemeGrayLight}" />
    <SolidColorBrush x:Key="ThemeGreenBrush" Color="{StaticResource ThemeGreen}" />
    <SolidColorBrush x:Key="ThemeGreenLightBrush" Color="{StaticResource ThemeGreenLight}" />

    <!-- 버튼 스타일 -->
    <Style x:Key="ButtonBaseStyle" TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            >
                        <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Hover -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border"
                                    Property="Background"
                                    Value="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>

                        <!-- Disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="RedButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonBaseStyle}">
        <Setter Property="Background" Value="{StaticResource ThemeRedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ThemeRedBrush}"/>
        <Setter Property="Tag" Value="{StaticResource ThemeRedLightBrush}"/>
    </Style>

    <Style x:Key="SlateGrayButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonBaseStyle}">
        <Setter Property="Background" Value="{StaticResource ThemeSlateGrayBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ThemeSlateGrayBrush}"/>
        <Setter Property="Tag" Value="{StaticResource ThemeSlateGrayLightBrush}"/>
    </Style>

    <Style x:Key="GrayButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonBaseStyle}">
        <Setter Property="Background" Value="{StaticResource ThemeGrayBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ThemeGrayBrush}"/>
        <Setter Property="Tag" Value="{StaticResource ThemeGrayLightBrush}"/>
    </Style>

    <Style x:Key="GreenButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonBaseStyle}">
        <Setter Property="Background" Value="{StaticResource ThemeGreenBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ThemeGreenBrush}"/>
        <Setter Property="Tag" Value="{StaticResource ThemeGreenLightBrush}"/>
    </Style>

    <!-- 원 버튼 스타일 -->
    <Style x:Key="CircleButtonBaseStyle" TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            CornerRadius="40"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}">
                        <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- RadioButton 스타일 -->
    <Style x:Key="RedRadioButtonStyle" TargetType="RadioButton">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource ThemeRedBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ThemeRedBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            CornerRadius="0">
                        <ContentPresenter
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>

                        <!-- Hover -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border"
                                    Property="Background"
                                    Value="{StaticResource ThemeRedLightBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>

                        <!-- Checked -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="border"
                                    Property="Background"
                                    Value="{StaticResource ThemeRedBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Normal"/>
                        </Trigger>

                        <!-- Disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- 정사각형 이미지 버튼 스타일 -->
    <Style x:Key="ImageButtonStyle" TargetType="Button">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="CardBorder" MaxWidth="300" MaxHeight="300"
                            Background="White" BorderBrush="#E0E0E0" BorderThickness="2">
                        <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="CardBorder" Property="BorderBrush" Value="#CCCCCC"/>
                            <Setter TargetName="CardBorder" Property="Background" Value="#F9F9F9"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="CardBorder" Property="BorderBrush" Value="{StaticResource ThemeRedBrush}"/>
                            <Setter TargetName="CardBorder" Property="BorderThickness" Value="2"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>